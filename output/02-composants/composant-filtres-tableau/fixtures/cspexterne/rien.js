/* Asset externe de la fixture rouge : reference reelle, lisible, mais qui n'appelle
   ni init() ni initAll() — la seule presence d'un <script src> ne doit jamais suffire
   a satisfaire G3. */
console.log('table-filters charge, non initialise');
