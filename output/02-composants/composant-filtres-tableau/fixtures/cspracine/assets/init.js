/* Asset externe de demonstration — RS-1 bis (TF-0837) : une application SERVIE declare ses
   assets depuis la racine web (« /assets/init.js »), pas relativement au fichier de la page.
   L'initialisation du composant vit ICI, et la page qui la reference n'a rien a dupliquer
   inline sous nonce. */
DigitAITableFilters.initAll();
